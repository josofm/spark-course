from setuptools import setup, find_packages
setup(
    name = "spark-course",
    version = "latest",
    description = ("udemy coruse to learn spark"),
    packages=find_packages(),
    include_package_data=True
)
